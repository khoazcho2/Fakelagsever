services:
  - type: web
    name: hoquocdev-server
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: ADMIN_SECRET
        value: hoquocdev123
